@model IEnumerable<FilRouge.Domain.Entities.Ratio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomRatio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomRatio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdRatio }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdRatio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdRatio })
        </td>
    </tr>
}

</table>
